import useScreenType from "../lib/room/hooks/useScreenType";

const AppLayout = () => {
  const screenType = useScreenType();

  let content = null;

  if (screenType == "mobile") {
  
  } else {
    
  }
};

export default AppLayout;
